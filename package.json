{
  "name": "nimiq-staking-miniapp",
  "version": "0.1.0",
  "description": "One-tap NIM staking inside Nimiq Pay.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "gate": "node --env-file=.env scripts/gate.js",
    "start": "node --env-file-if-exists=.env src/server.js"
  },
  "dependencies": {
    "@nimiq/core": "^2.20.0",
    "@nimiq/mini-app-sdk": "^0.1.0"
  }
}
